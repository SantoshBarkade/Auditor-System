# ==============================================================================
# NEXORA Frontend Environment Configuration (.env.example)
# ==============================================================================
# In local development, leave this empty to use Vite's local dev proxy (localhost:8000).
# In production on Vercel, set this to your deployed Render backend service URL.

VITE_API_BASE_URL=https://your-backend-service.onrender.com
